server.port=${PORT:8080}

# Clean JDBC Connection String (No inline credentials)
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://ep-fancy-morning-aea9saug-pooler.c-2.us-east-2.aws.neon.tech/neondb?sslmode=require}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:neondb_owner}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:npg_s05ZJOhpEfHD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.jdbc.batch_size=20

# JWT Configuration
security.jwt.secret=keystone-super-secret-key-minimum-32-chars-long
security.jwt.expiry=86400000

# Application Settings
spring.application.name=keystone
server.error.include-message=always
server.error.include-binding-errors=always
